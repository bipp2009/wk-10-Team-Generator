/*Declare the variables used in the node index file: inquirer used to generate questions, fs to reference file system, utils to ref the utilities used, 
and team generator to use the folder file
*/
const inquirer = require("inquirer");
const fs = require('fs');
const util = require('util');
const generatehtml = require("./utils/generatehtml");
//SEtup inquirer to ask questions

//Setup application generate code flow

//Plan out the application flow

//Setup the models/classes for manager, engineer and intern
//shared properties for each employee class some questions are unie to manager intern etc
// new folder for classes = 4 files employee, manager engineer and intern
